name: Publish Packages
on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      tag_name:
        description: Version to be published e.g. 1.0.0
        type: string
jobs:
  Build-And-Publish:
    runs-on: ubuntu-latest
    env:
      NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version-file: '.nvmrc'
          cache: 'npm'
          cache-dependency-path: '**/package-lock.json'
          registry-url: 'https://registry.npmjs.org'
      - run: npm ci
      - run: npm --no-git-tag-version version ${{ github.event.release.tag_name || inputs.tag_name }} --workspaces
      - run: npm run build --workspace=@hub-fx/core
      - run: npm publish --workspace=@hub-fx/core --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
      - run: npm install @hub-fx/core@${{ github.event.release.tag_name || inputs.tag_name }} --workspace=@hub-fx/forms
      - run: npm run build --workspace=@hub-fx/forms
      - run: npm publish --workspace=@hub-fx/forms --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
      - run: npm install @hub-fx/core@${{ github.event.release.tag_name || inputs.tag_name }} --workspace=@hub-fx/examples
      - run: npm run build --workspace=@hub-fx/examples
      - run: npm publish --workspace=@hub-fx/examples --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
      # - run: npm install @hub-fx/forms@${{ github.event.release.tag_name || inputs.tag_name }} --workspace=@hub-fx/react-forms
      # - run: npm run build --workspace=@hub-fx/react-forms
      # - run: npm publish --workspace=@hub-fx/react-forms --access public
      #   env:
      #     NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
