{
  "name": "@reactables/web-workers",
  "description": "Web Worker Decorators for Reactables",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rimraf dist && rollup --config",
    "lint": "eslint --max-warnings 0 \"src/**/*.ts*\" && prettier --check src/",
    "fix": "eslint --fix \"src/**/*.ts*\" && prettier --write \"src/**/*.(ts*|scss)\""
  },
  "author": "David Lai",
  "license": "ISC",
  "peerDependencies": {
    "rxjs": "^6.0.0 || ^7.0.0"
  }
}
